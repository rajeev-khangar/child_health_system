<div class="pull-right">
    <%= link_to "New manager", new_manager_path, class: "btn btn-success" %>
</div>
  <div class="container">
    <div class="container index-container">
      <div class="col-md-12">
        <h1>Fathers List</h1>
      </div>
      <br>
    <div class="col-md-12">
      <table class="table table-stripped">
        <thead>  
          <tr>
            <th>#</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Address</th>
            <th>Action</th>
            
          </tr>
        </thead> 
        <tbody>
          <% @fathers.each do |father| %>
            <tr>
              <td><%= father.id %></td>
              <td><%= father.first_name %></td> 
              <td><%= father.last_name %></td>
              <td><%= father.email %></td>
              <td><%= father.physical_address %></td>      
              <td ><%= link_to '',  edit_father_path(father), class: "btn btn-default glyphicon glyphicon-edit" %>
              <%= link_to'',  father_path(father), class: "btn btn-default glyphicon glyphicon-eye-open" %>
              <%= link_to '', father_path(father),
              method: :delete,
              data: { confirm: 'Are you sure?' }, class: "btn btn-default glyphicon glyphicon-trash" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>      
    </div>    
  </div>
</div>




